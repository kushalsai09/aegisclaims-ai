# Synthetic data

Phase 2's versioned fictional product, deterministic scenario manifest, and browser-upload fixtures live here. `insurance_platform.seed` renders the manifest documents deterministically and stores them through the configured object-storage adapter. No real PII or proprietary carrier material is permitted.
